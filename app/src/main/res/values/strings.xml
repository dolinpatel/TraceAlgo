<resources>
    <string name="app_name">TraceAlgo</string>
    <string name="selectionsortalgo"><![CDATA[void selectionSort(int arr[], int n){\n
    \tint i, j, minIndex, tmp;\n
    \tfor (i = 0; i < n - 1; i++) {\n
    \tminIndex = i;\n
    \tfor (j = i + 1; j < n; j++)\n
    \t\tif (arr[j] < arr[minIndex])\n
    \t\t\tminIndex = j;\n
    \t\t\tif (minIndex != i) {\n
    \t\t\t\ttmp = arr[i];\n
    \t\t\t\tarr[i] = arr[minIndex];\n
    \t\t\t\tarr[minIndex] = tmp;\n             \t\t\t}\n
    \t\t}\n }]]></string>
    <string name="selectalgo">Step 1 − Set MIN to location 0 \nStep 2 − Search the minimum element in the list Step 3 − Swap with value at location MIN Step 4 − Increment MIN to point to next element Step 5 − Repeat until list is sorted</string>
    <string-array name="selecalgo">void cv</string-array>
</resources>
